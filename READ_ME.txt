1. To execute the program first go to the directory that contains the source file.

2. In terminal it appears like: \home\workspace\KMeans_Visual\src and in CMD
it appears like: cd C:\Users\Public\workspace\KMeans_Visual\src

3. Once you have reached to the folders from either of the two methods(Terminal is used for
Linux) type javac KMeans.java
 Same command goes for terminal as well

5. Once this is done we can execute the program by
typing java KMeans (Applies in terminal as well)

6. Provide the value for K and number of iterartions and record the random values generated
by the program. 